package ejercicios;

import java.util.Scanner;

public class Fantasmones {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int y = sc.nextInt() -1 ;
        int x = sc.nextInt() -1 ;        
        String[][] matriu = new String[y][x];
        matriu = JCllenarmatriz(y, x, sc); 
        int k = sc.nextInt();
        int yb = 0;
        int xb = 0;

        for(int i = 0; i < k; i++) {
        	int y2 = sc.nextInt();
        	int x2 = sc.nextInt();
        	if(i == 0) {
        		matriu[y2][x2] = "b";
        		yb = y2;
        		xb = x2;}
        	else {
            	matriu[y2][x2] = "1";}

        		
        	}
        for (int dx = -1; dx <= 1; dx++) {
            for (int dy = -1; dy <= 1; dy++) {
                if (dx == 0 && dy == 0) {
                    
                } else {
                    int nx = xb + dx;
                    int ny = yb + dy;
                    if (esticDins(nx, ny, matriu) && matriu[nx][ny] == 2) {
                        break;
                    }
                }
            }
        }
        	
        

        }
        
    
    
    public static String[][] JCllenarmatriz(int filas, int cols, Scanner sc) {
        String[][] matriu = new String[filas][cols];
        for (int i = 0; i < matriu.length; i++) {
            for (int w = 0; w < matriu[0].length; w++) {
                matriu[i][w] = "0";
            }
        }
        return matriu;
    }
    public static boolean esticDins(int f, int c, String[][] matriu) {
        return (f >= 0 && f < matriu.length && c >= 0 && c < matriu[0].length) || matriu[f][c].equals("1");
    }
}