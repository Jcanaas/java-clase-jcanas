	package ejercicios;
	
	import java.util.*;
	
	public class alfil {
	    public static void main(String[] args) {
	        Scanner sc = new Scanner(System.in);
	        int n = sc.nextInt();
	        int[][]  matriu = new int[8][8];
	        sc.nextLine();
	        for (int i = 0; i < n; i++) {
	        String s = sc.nextLine();
	        int f = s.charAt(1) - '0';	        
	        int c = con(s.charAt(0));
	        
	        
	        int suma = 0;
	        while(f <= 7 && c <= 7 && f >= 0 && c >= 0) {
	        	suma++;
	        	f++;
	        	c++;
	        }
	        
	        f = s.charAt(1) - '0';	        
	        c = con(s.charAt(0));
	        
	        System.out.println(suma);	

	        while(f <= 7 && c <= 7 && f >= 0 && c >= 0) {
	        	suma++;
	        	f++;
	        	c--;
	        }
	        
	        
	        f = s.charAt(1) - '0';	        
	        c = con(s.charAt(0));
	        
	        System.out.println(suma);	

	        while(f <= 7 && c <= 7 && f >= 1 && c >= 1) {
	        	suma++;
	        	f--;
	        	c--;
	        }
	        
	        f = s.charAt(1) - '0';	       
	        c = con(s.charAt(0));
	       
	        System.out.println(suma);	

	        while(f <= 7 && c <= 7 && f >= 0 && c >= 0) {
	        	suma++;
	        	f--;
	        	c++;
	        }
	        
	        f = s.charAt(1) - '0';	        
	        c = con(s.charAt(0));
	        	
	        System.out.println(suma);	
	        }	        
	    }
	    static int con(char c) {
	        if(c == 'a') return 1;
	        else if(c == 'b') return 2;
	        else if(c == 'c') return 3;
	        else if(c == 'd') return 4;
	        else if(c == 'e') return 5;
	        else if(c == 'f') return 6;
	        else if(c == 'g') return 7;
	        else if(c == 'h') return 8;
	        return 0;
	    }
	}
