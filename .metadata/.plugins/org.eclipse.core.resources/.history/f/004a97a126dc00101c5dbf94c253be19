package concurso;

import java.util.*;

public class Ex1 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        List<Integer> lista = new ArrayList<>();
       int Tx = sc.nextInt();
       int Ty = sc.nextInt();
       String[][]  casillas = new String[Ty][Tx];
       String[][]  casillas2 = new String[Ty][Tx];

       for (int i = 0; i < Ty; i++) {
    	   String a = sc.nextLine();
           for (int w = 0; w < Tx; w++) {
    	   casillas[i][w] = Character.toString(a.charAt(w));
           }
       }
       for (int i = 0; i < Ty; i++) {
    	   String a = sc.nextLine();
           for (int w = 0; w < Tx; w++) {
    	   casillas2[i][w] = Character.toString(a.charAt(w));
           }
       }
       
       
    }
}