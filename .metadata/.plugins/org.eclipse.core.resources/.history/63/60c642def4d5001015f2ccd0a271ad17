package ejercicios;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Scanner;
import java.util.Set;


public class listamutant {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int casos = sc.nextInt();
        int tamany = sc.nextInt();
        for (int t = 0; t < casos; t++) {
            List<Integer> pokemons = new ArrayList<>();
            List<Integer> nivel = new ArrayList<>();
            for (int i = 0; i < tamany; i++) pokemons.add(sc.nextInt());
            for (int i = 0; i < tamany; i++) nivel.add(sc.nextInt());

            // Crear las listas que usaremos para imprimir
            List<Integer> pokemonIds = new ArrayList<>();
            List<Integer> nivelesMax = new ArrayList<>();

            
            //recorremos toda la lista y aplicamos una serie de criterios para definir cual de los dos tien ele nivel mas alto y añadimiso este a la lista que imprimiremos
            for (int i = 0; i < tamany; i++) {
                int id = pokemons.get(i);
                int lvl = nivel.get(i);
                if (!pokemonIds.contains(id)) {
                    pokemonIds.add(id);
                    nivelesMax.add(lvl);
                } else {
                    int idx = pokemonIds.indexOf(id);
                    if (lvl > nivelesMax.get(idx)) {
                        nivelesMax.remove(idx);
                        pokemonIds.remove(idx);
                        nivelesMax.add(lvl);
                        pokemonIds.add(id);
                        
                    }
                }
            }
            //Bucle para imprimir toos los valores
            for(int w = 0; w < pokemonIds.size(); w++) {
            	System.out.println(pokemonIds.get(w)+ " ");
            }
            
            
        }
        sc.close();
    }
}