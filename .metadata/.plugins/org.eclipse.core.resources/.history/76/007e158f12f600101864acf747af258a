package ejercicios;

import java.util.*;

public class Black_Friday {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int x = sc.nextInt();
        int y = sc.nextInt();
        int[][] matriu = new int[y][x];
        for(int i = 0; i < y; i++) {
        	for(int w = 0; w < y; w++) {
        		matriu[i][w] = sc.nextInt();
        	}
        }
        int aug = sc.nextInt();
        for(int i = 0; i < y; i++) {
        	for(int w = 0; w < y; w++) {
        		matriu[i][w] = matriu[i][w] * 3;
        	}
        }
        for(int i = 0; i < y; i++) {
        	for(int w = 0; w < y; w++) {
        		System.out.println();
        	}
        }
                  		
    }
}