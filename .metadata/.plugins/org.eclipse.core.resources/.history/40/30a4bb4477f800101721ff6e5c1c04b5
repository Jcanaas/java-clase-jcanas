package ejercicios;

import java.util.Scanner;

public class PUTOS_FURROS {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int x = 5;
        int y = 5;
        String[][] matriu = new String[y][x];
        
        matriu = JCllenarmatriz(y, x, sc);
        String r = "SI";
        for (int i = 0; i < matriu.length; i++) {
			for (int w = 0; w < (matriu[0].length); w++) {
				
			
        for (int dx = -1; dx <= 1; dx++) {
            for (int dy = -1; dy <= 1; dy++) {
                if (dx == 0 && dy == 0) {
                    
                } else {
                    int nx = x + dx;
                    int ny = y + dy;
                    if (esticDins(nx, ny, matriu)) {
                        if(matriu[i][w] == "A" && matriu[ny][nx] == "B" ) {
                        	r = "NO";
                        }
                        if(matriu[i][w] == "C" && matriu[ny][nx] == "D" ) {
                        	r = "NO"; 	
                        }
                    }
                }
            }
        }
			}
			
		}
        System.out.println(r);
    }
    
    public static String[][] JCllenarmatriz(int filas, int cols, Scanner sc) {
        String[][] matriu = new String[filas][cols];
        for (int i = 0; i < matriu.length; i++) {
            for (int w = 0; w < matriu[0].length; w++) {
                matriu[i][w] = sc.next();
            }
        }
        return matriu;
    }
    public static boolean esticDins(int f, int c, String[][] matriu) {
        return f >= 0 && f < matriu.length && c >= 0 && c < matriu[0].length;
    }
}